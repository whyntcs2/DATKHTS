[env:esp32doit-devkit-v1]
platform = espressif32
board = esp32doit-devkit-v1
framework = arduino

monitor_speed = 115200
monitor_filters = time

; LD2450 uses UART2 at 256000 baud inside src/main.cpp.
; Change RADAR_RX_PIN / RADAR_TX_PIN in src/main.cpp if your wiring differs.
